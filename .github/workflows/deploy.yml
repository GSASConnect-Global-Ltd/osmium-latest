name: 🚀 Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🚀 Deploy to Server
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.VPS_IP }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 25m
          script: |
            set -e  # Exit on any error
            
            echo "🚀 Starting deployment..."
            
            # ================================================================
            # 1. SETUP PATH FOR NODE.JS
            # ================================================================
            echo "📊 Setting up Node.js PATH..."
            
            # Primary nvm path
            NVM_PATH="/home/admin-gsas/.nvm/versions/node/v24.13.0/bin"
            
            if [ -d "$NVM_PATH" ]; then
              echo "✅ Found Node.js at: $NVM_PATH"
              export PATH="$NVM_PATH:$PATH"
              
              # Create symlinks
              sudo ln -sf "$NVM_PATH/node" /usr/local/bin/node 2>/dev/null || true
              sudo ln -sf "$NVM_PATH/npm" /usr/local/bin/npm 2>/dev/null || true
              sudo ln -sf "$NVM_PATH/npx" /usr/local/bin/npx 2>/dev/null || true
            else
              echo "🔍 Searching for Node.js..."
              # Try common locations
              for path in "/usr/local/bin" "/usr/bin" "/opt/node/bin" "/home/admin-gsas/.nvm/versions/node/*/bin"; do
                if [ -f "$path/npm" ]; then
                  echo "✅ Found npm at: $path"
                  export PATH="$path:$PATH"
                  break
                fi
              done
            fi
            
            # Verify
            echo "📊 Node.js: $(node --version 2>/dev/null || echo 'Not found')"
            echo "📊 npm: $(npm --version 2>/dev/null || echo 'Not found')"
            
            if ! command -v npm &> /dev/null; then
              echo "❌ npm not found! Installing..."
              curl -fsSL https://deb.nodesource.com/setup_24.x | sudo -E bash -
              sudo apt install -y nodejs
            fi
            
            # ================================================================
            # 2. CONFIGURATION
            # ================================================================
            APP_NAME="orrel-app"
            APP_DIR="/var/www/orrel-app"
            BACKUP_DIR="/home/admin-gsas/backups"
            
            echo "📱 Application: $APP_NAME"
            echo "📁 Directory: $APP_DIR"
            
            # Create backup directory
            mkdir -p "$BACKUP_DIR"
            
            # ================================================================
            # 3. GO TO APP DIRECTORY
            # ================================================================
            echo "📁 Changing to app directory..."
            if [ ! -d "$APP_DIR" ]; then
              echo "📦 Creating app directory..."
              sudo mkdir -p "$APP_DIR"
              sudo chown -R admin-gsas:admin-gsas "$APP_DIR"
            fi
            
            cd "$APP_DIR" || {
              echo "❌ Failed to access $APP_DIR"
              exit 1
            }
            
            echo "✅ Current directory: $(pwd)"
            
            # ================================================================
            # 4. CREATE BACKUP
            # ================================================================
            echo "📦 Creating backup..."
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)
            BACKUP_FILE="$BACKUP_DIR/${APP_NAME}_backup_$TIMESTAMP.tar.gz"
            
            if [ -d ".git" ] || [ -f "package.json" ]; then
              tar --exclude='node_modules' --exclude='.next' -czf "$BACKUP_FILE" . 2>/dev/null && \
                echo "✅ Backup created: $BACKUP_FILE" || \
                echo "⚠️ Backup creation had issues"
            else
              echo "ℹ️ First deployment - no backup needed"
            fi
            
            # ================================================================
            # 5. UPDATE CODE
            # ================================================================
            echo "⬇️ Updating code..."
            
            if [ -d ".git" ]; then
              git fetch origin
              git reset --hard origin/main
              # git clean -fd
              git clean -fd -e .lockfile.hash
              echo "✅ Code updated"
            else
              echo "📦 Cloning repository..."
              git clone https://github.com/GSASConnect-Global-Ltd/osmium-latest.git .
            fi
            
            # ================================================================
            # 6. DEPENDENCY INSTALLATION (SIMPLIFIED)
            # ================================================================
            echo "📦 Managing dependencies..."

            # Create hash of package-lock.json if it exists

            if [ -f "package-lock.json" ]; then
                CURRENT_HASH=$(sha256sum package-lock.json | cut -d' ' -f1)

                # Check stored hash

                if [ -f ".lockfile.hash" ]; then
                  STORED_HASH=$(cat .lockfile.hash)
                  if [ "$CURRENT_HASH" = "$STORED_HASH" ]; then
                    echo "✅ Dependencies unchanged, skipping install"
                    SKIP_INSTALL=true
                  else
                    echo "📦 package-lock.json changed, installing..."
                    SKIP_INSTALL=false
                  fi

                else 
                  echo "📦 First install or no hash file"
                  SKIP_INSTALL=false
                fi
            else
                echo "📦 No package-lock.json, installing..."
                SKIP_INSTALL=false
            fi

            if [ "$SKIP_INSTALL" = false ]; then
                # Clean old installation
                [ -d "node_modules" ] && echo "🧹 Removing old node_modules..." && rm -rf node_modules
                
                # Install
                echo "📥 Installing dependencies..."
                if [ -f "package-lock.json" ]; then
                  npm ci --include=dev --no-audit --no-fund
                else
                  npm install --include=dev --no-audit --no-fund
                fi

                # Save hash for next time

                if [ -f "package-lock.json" ]; then
                  sha256sum package-lock.json | cut -d' ' -f1 > .lockfile.hash
                fi
                echo "✅ Dependencies installed"
            fi

            # Ensure TypeScript is available

            echo "📦 Ensuring TypeScript for build..."

            npm list typescript 2>/dev/null | grep -q typescript || npm install typescript @types/node --no-audit --no-fund

            # Always remove .next for fresh build
            echo "🧹 Cleaning previous build..."
            rm -rf .next




            # ================================================================
            # 7. SET ENVIRONMENT VARIABLES
            # ================================================================
            echo "🔐 Setting up environment variables..."
            
            # Create .env.production file with GitHub Secrets
            cat > "$APP_DIR/.env.production" << EOF
            # Generated by GitHub Actions - DO NOT EDIT MANUALLY
            OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
            NODE_ENV=production
            NEXT_PUBLIC_API_BASE_URL=https://orrelng.com
            PORT=3002
            NEXT_TELEMETRY_DISABLED=1
            # Add other secrets as needed
            EOF
            
            # Set correct permissions
            chmod 600 "$APP_DIR/.env.production"
            
            echo "✅ Environment variables set"
            
            # ================================================================
            # 7. BUILD APPLICATION
            # ================================================================
            echo "🏗️ Building application..."
            
            export NODE_ENV=production
            export NEXT_TELEMETRY_DISABLED=1
            
            if npm run build; then
              echo "✅ Build successful"
              
              # Check build output
              if [ -d ".next" ]; then
                BUILD_SIZE=$(du -sh .next | cut -f1)
                echo "📦 Build size: $BUILD_SIZE"
              fi
            else
              echo "❌ Build failed!"
              
              # Rollback
              if [ -f "$BACKUP_FILE" ]; then
                echo "🔄 Rolling back from backup..."
                rm -rf .next node_modules
                tar -xzf "$BACKUP_FILE" 2>/dev/null || echo "Rollback failed"
              fi
              
              exit 1
            fi
            
            # Install production-only dependencies
            # echo "⚡ Installing production dependencies..."
            # npm ci --omit=dev --no-audit --no-fund || {
            #   echo "⚠️ Production install failed, continuing..."
            # }
            
            # ================================================================
            # 8. PM2 SETUP
            # ================================================================
            echo "⚙️ Setting up PM2..."
            
            pm2 delete "$APP_NAME" 2>/dev/null || true

            # Determine how to start

            if [ -f ".next/standalone/server.js" ]; then
                echo "🔐 Starting standalone build..."
                cd .next/standalone
                pm2 start server.js --name "$APP_NAME" -- --port 3000 --hostname 0.0.0.0
                cd "$APP_DIR"
            else
                echo "⚡ Starting Next.js production server..."
                # CRITICAL: Use npx next start, not npm start
                pm2 start "npx next start" --name "$APP_NAME" -- --port 3000 --hostname 0.0.0.0
            fi

            pm2 save 2>/dev/null || true

            # ================================================================
            # 9. VERIFICATION
            # ================================================================

            echo "🔍 Verifying startup..."
            sleep 10

            # Check if running
            if pm2 status "$APP_NAME" | grep -q "online"; then
                echo "✅ PM2 process is online"
            else
                echo "❌ PM2 process not started"
                pm2 logs "$APP_NAME" --lines 20
                exit 1
            fi

            # Check if listening
            if timeout 3 bash -c "echo >/dev/tcp/127.0.0.1/3000"; then
                echo "✅ Port 3000 is listening"
            else
                echo "❌ Port 3000 not listening"
                exit 1

            fi

            # Health check
            echo "🏥 Final health check..."
            if curl -s -f -m 10 http://127.0.0.1:3000 > /dev/null; then
                echo "🎉 Application is fully operational!"
            else
                echo "⚠️ Application started but not responding to HTTP"
                echo "   Testing alternative paths..."
                curl -I http://127.0.0.1:3000 2>/dev/null | head -1
                echo "   Check: curl http://127.0.0.1:3000"
            fi
            
            # # ================================================================
            # # 9. HEALTH CHECK
            # # ================================================================
            # echo "🏥 Running health check..."
            # sleep 5  # Give app time to start
            
            # MAX_RETRIES=3
            # HEALTHY=false
            
            # for i in $(seq 1 $MAX_RETRIES); do
            #   if curl -s -f -m 5 http://127.0.0.1:3000 > /dev/null; then
            #     echo "✅ Application is healthy (attempt $i/$MAX_RETRIES)"
            #     HEALTHY=true
            #     break
            #   else
            #     echo "  Waiting... (attempt $i/$MAX_RETRIES)"
            #     sleep 3
            #   fi
            # done
            
            # if [ "$HEALTHY" = false ]; then
            #   echo "⚠️ Health check failed after $MAX_RETRIES attempts"
            #   echo "📋 Checking PM2 logs..."
            #   pm2 logs "$APP_NAME" --lines 10 2>/dev/null || echo "No logs available"
            # fi
            
            # # ================================================================
            # # 10. CLEANUP
            # # ================================================================
            # echo "🧹 Cleaning up old backups..."
            # cd "$BACKUP_DIR"
            
            # # Keep last 5 backups
            # ls -t ${APP_NAME}_backup_*.tar.gz 2>/dev/null | tail -n +6 | while read -r file; do
            #   echo "  Removing: $file"
            #   rm -f "$file" 2>/dev/null || true
            # done
            
            # BACKUP_COUNT=$(ls -1 ${APP_NAME}_backup_*.tar.gz 2>/dev/null | wc -l)
            # echo "📦 Backups retained: $BACKUP_COUNT"
            
            # # ================================================================
            # # 11. FINAL STATUS
            # # ================================================================
            # echo ""
            # echo "🎉 DEPLOYMENT COMPLETED SUCCESSFULLY!"
            # echo "========================================"
            # echo "📱 Application: $APP_NAME"
            # echo "📁 Directory: $APP_DIR"
            # echo "🌐 URL: https://orrelng.com"
            # echo "🕒 Time: $(date)"
            # echo "📊 Node.js: $(node --version)"
            # echo "📊 npm: $(npm --version)"
            # echo ""
            # echo "📋 PM2 Status:"
            # pm2 status "$APP_NAME" --silent 2>/dev/null || {
            #   echo "  Could not get PM2 status"
            #   echo "  Checking process directly..."
            #   ps aux | grep -v grep | grep "$APP_NAME" || echo "  No process found"
            # }
            
            # echo ""
            # echo "✅ Deployment complete! The application should now be live."

